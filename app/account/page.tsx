// account/page.tsx
import AccountPageClient from "./AccountPageClient";

export default function AccountPage() {
  // Any server–side data fetching (e.g. cookies, session info) could be done here.
  // For now, we simply render the client component.
  return <AccountPageClient />;
}
